<div class="container">
  <div class="card p-3 m-3">
    <h1><%= @offer.title %></h1>
    <h2><%= @offer.user.name %></h2>
    <p>Offer description: <%= @offer.description %></p>

    <div class="d-flex">
      <%= link_to 'book now', new_offer_booking_path(@offer), class: "btn btn-warning m-3" %>
    </div>

  </div>
</div>


